{
  "name": "guestpass-functions",
  "description": "Cloud Functions for Firebase backend supporting GuestPass",
  "version": "0.1.0",
  "private": true,
  "main": "lib/index.js",
  "scripts": {
    "build": "tsc",
    "clean": "rimraf lib",
    "deploy": "npm run build && firebase deploy --only functions",
    "emulate": "npm run build && firebase emulators:start --only functions",
    "lint": "eslint --ext .ts src"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "@google-cloud/storage": "^7.17.1",
    "firebase-admin": "^12.7.0",
    "firebase-functions": "^4.9.0",
    "pdf-lib": "^1.17.1",
    "qrcode": "^1.5.4"
  },
  "devDependencies": {
    "@types/node": "^22.10.5",
    "@types/qrcode": "^1.5.5",
    "eslint": "^9.9.0",
    "eslint-config-google": "^0.14.0",
    "rimraf": "^6.0.1",
    "typescript": "^5.6.3"
  }
}
